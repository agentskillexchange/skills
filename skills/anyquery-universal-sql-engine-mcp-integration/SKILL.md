---
name: "Anyquery Universal SQL Engine with MCP Integration"
description: "Anyquery is a SQL query engine that lets you run SQL against 40+ apps, files, and databases including GitHub, Notion, Chrome, and Apple Notes. Built on SQLite with MCP server support for connecting AI agents to structured data across services."
category: "Data Extraction & Transformation"
framework: "MCP-compatible"
verification: listed
rating: 0
reviews: 0
creator: ""
creator_handle: ""
creator_verified: false
source: "https://agentskillexchange.com/skills/anyquery-universal-sql-engine-mcp-integration/"
---
# Anyquery Universal SQL Engine with MCP Integration

Anyquery is a SQL query engine that lets you run SQL against 40+ apps, files, and databases including GitHub, Notion, Chrome, and Apple Notes. Built on SQLite with MCP server support for connecting AI agents to structured data across services.

## Installation

### Any AI Agent (npx)
```bash
npx skills add "anyquery-universal-sql-engine-mcp-integration"
```

### Claude Code
```bash
npx skills add "anyquery-universal-sql-engine-mcp-integration" --claude-code
```

### Cursor
```bash
npx skills add "anyquery-universal-sql-engine-mcp-integration" --cursor
```

### Codex
```bash
npx skills add "anyquery-universal-sql-engine-mcp-integration" --codex
```

### OpenClaw
```bash
clawhub install "anyquery-universal-sql-engine-mcp-integration"
```

## Source

- [Anyquery Universal SQL Engine with MCP Integration on Agent Skill Exchange](https://agentskillexchange.com/skills/anyquery-universal-sql-engine-mcp-integration/)
