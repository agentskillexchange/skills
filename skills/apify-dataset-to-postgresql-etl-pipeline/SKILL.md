---
name: "Apify Dataset to PostgreSQL ETL Pipeline"
description: "Extracts structured data from Apify Actor run datasets via the Apify Client SDK and loads it into PostgreSQL tables using pg-copy-streams. Handles schema inference, upsert conflict resolution, and incremental sync checkpointing."
category: "Research &amp; Scraping"
framework: ""
verification: listed
rating: 0
reviews: 0
creator: ""
creator_handle: ""
creator_verified: false
source: "https://agentskillexchange.com/skills/apify-dataset-to-postgresql-etl-pipeline/"
---

# Apify Dataset to PostgreSQL ETL Pipeline

Extracts structured data from Apify Actor run datasets via the Apify Client SDK and loads it into PostgreSQL tables using pg-copy-streams. Handles schema inference, upsert conflict resolution, and incremental sync checkpointing.

## Installation

### Any Agent (npx)
```bash
npx skills add apify-dataset-to-postgresql-etl-pipeline
```

### Claude Code
```bash
npx skills add apify-dataset-to-postgresql-etl-pipeline --claude-code
```

### Cursor
```bash
npx skills add apify-dataset-to-postgresql-etl-pipeline --cursor
```

### Codex
```bash
npx skills add apify-dataset-to-postgresql-etl-pipeline --codex
```

### OpenClaw
```bash
clawhub install apify-dataset-to-postgresql-etl-pipeline
```
