---
name: "Build Java agent transport gateways with Atmosphere"
slug: "build-java-agent-transport-gateways-with-atmosphere"
description: "Use Atmosphere to build JVM agent gateways that stream over WebSocket, SSE, gRPC, WebTransport, MCP, A2A, and AG-UI with governance hooks."
github_stars: 3765
verification: "listed"
source: "https://github.com/Atmosphere/atmosphere"
author: "Atmosphere"
publisher_type: "organization"
category: "Developer Tools"
framework: "Multi-Framework"
tool_ecosystem:
  github_repo: "Atmosphere/atmosphere"
  github_stars: 3765
---

# Build Java agent transport gateways with Atmosphere

Use Atmosphere to build JVM agent gateways that stream over WebSocket, SSE, gRPC, WebTransport, MCP, A2A, and AG-UI with governance hooks.

## Prerequisites

Java/JVM project, Atmosphere CLI or libraries, chosen agent runtime adapter, target transport modules

## Installation

Choose whichever fits your setup:

1. Copy this skill folder into your local skills directory.
2. Clone the repo and symlink or copy the skill into your agent workspace.
3. Add the repo as a git submodule if you manage shared skills centrally.
4. Install it through your internal provisioning or packaging workflow.
5. Download the folder directly from GitHub and place it in your skills collection.

Install command or upstream instructions:

```
Install the Atmosphere CLI with Homebrew or the official install script, scaffold an agent with atmosphere new, choose the runtime adapter and transport modules, then run the service with atmosphere run.
```

## Documentation

- https://async-io.live

## Source

- [Agent Skill Exchange](https://agentskillexchange.com/skills/build-java-agent-transport-gateways-with-atmosphere/)
